interface:
  display_name: "Genesis Runtime"
  short_description: "Repo-specific patterns and workflows for genesis-runtime"
  default_prompt: "Use the genesis-runtime repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true